import { CourseDateHoverDirective } from './course-date-hover.directive';

describe('CourseDateHoverDirective', () => {
  it('should create an instance', () => {
    const directive = new CourseDateHoverDirective();
    expect(directive).toBeTruthy();
  });
});
